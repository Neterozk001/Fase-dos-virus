// Evento: Mouse Left Pressed no obj_btn_submit

with (obj_game_controller_1) {
    var resultado = compile_code(); // Chama a função de depurar o codigo
    
    if (resultado == true) {
        // Se o codigo estiver certo, executa alguma logica
    } else {
        // Se o codigo estiver errado, executa alguma logica
    }
}